name: Update all versions (scheduled)

on:
  schedule:
    - cron: "37 4 * * 5"

permissions:
  contents: write
  issues: write

jobs:
  update-linux:
    uses: ./.github/workflows/update-version.yml
    with:
      platform: linux
